import { Button } from '@quec/panel-base-ui';
import React from 'react';
import { View } from 'react-native';

export const ButtonPlay = () => {
    return (
        <View>
            <Button title={'测试1234'} onPress={() => {}} />
        </View>
    );
};
