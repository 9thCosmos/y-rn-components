import { ToastUsed, ToastUsedProps, ToastUsedRef } from './Toast';

// TODO 类型定义
export const Toast: any = Object.assign(ToastUsed, {
    // Base: ToastBase,
});
export type { ToastUsedRef, ToastUsedProps };
