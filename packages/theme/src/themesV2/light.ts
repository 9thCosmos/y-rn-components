import { QPThemeConfig } from '../types';

export const QPLightThemeConfig: QPThemeConfig = {
    brandColor: '#0091ff',
    successColor: '#00cf63',
    warningColor: '#ff6600',
    errorColor: '#ff4219',
};
