import { QPThemeConfig } from '../types';

export const QPDarkThemeConfig: QPThemeConfig = {
    type: 'dark',
    brandColor: '#0091ff',
    successColor: '#00cf63',
    warningColor: '#ff6600',
    errorColor: '#ff4219',
};
