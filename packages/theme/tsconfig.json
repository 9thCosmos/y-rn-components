{
    "extends": "../../tsconfig",
    "compilerOptions": {
        "allowJs": true /* Allow javascript files to be compiled. */,
        "outDir": "dist",
        "declaration": true,
        "composite": true,
        "rootDir": "src",
        "types": ["vitest/importMeta"]
    },
    "include": ["src/**/*"],
    "exclude": ["dist"]
}
